using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SophieScript : MonoBehaviour
{
    // Start is called before the first frame update
    public static GameObject notMorphedPrefab;

    void Start()
    {
        notMorphedPrefab = this.gameObject;
    }

    // Update is called once per frame
    void Update()
    {
    }
}
